import React, {useState} from 'react';

export const Tab = ({name, active=false, children, onClick}) => {
    

}



export default function AppTabLayout() {
    const [active, setActive] = useState(0);
    
    return (
        <div>
            
        </div>
    )
}
